{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildEnvironment": "V3",
    "watchPatterns": [
      "web/**"
    ],
    "buildCommand": "nix-env -iA nixpkgs.nodejs_20 nixpkgs.unzip nixpkgs.curl && curl -fsSL https://bun.sh/install | bash && export PATH=\"$HOME/.bun/bin:$PATH\" && cd web && bun install"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "startCommand": "bash -lc 'export PATH=\"$HOME/.bun/bin:$PATH\" && cd web && bun run server.ts'",
    "sleepApplication": false,
    "useLegacyStacker": false,
    "multiRegionConfig": {
      "us-west2": {
        "numReplicas": 1
      }
    },
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
