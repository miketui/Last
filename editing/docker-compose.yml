version: "3.8"

services:
  editor:
    build: .
    ports:
      - "3100:3100"
    env_file:
      - .env
    volumes:
      - editor-data:/app/data
    restart: unless-stopped

volumes:
  editor-data:
